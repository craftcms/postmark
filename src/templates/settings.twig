{% import "_includes/forms" as forms %}

{{ forms.autosuggestField({
  label: "Server Token"|t('postmark'),
  instructions: "The Postmark server API token."|t('postmark'),
  id: 'token',
  name: 'token',
  value: adapter.token,
  suggestEnvVars: true,
  errors: adapter.getErrors('token')
}) }}

{{ forms.autosuggestField({
  label: "Message Stream ID"|t('postmark'),
  instructions: "The Postmark Message Stream ID. (The default transactional Message Stream will be used if this is left blank.)"|t('postmark'),
  id: 'messageStream',
  name: 'messageStream',
  value: adapter.messageStream,
  suggestEnvVars: true,
  errors: adapter.getErrors('messageStream')
}) }}
